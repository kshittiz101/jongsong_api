DEBUG=True
SECRET_KEY=django-insecure-6!obttbzn04x&xdb$nz+kdgi5a8b(0#jcscc_o&y#u%qmb)zd7
ALLOWED_HOSTS=localhost,127.0.0.1

# Optional: override paths (Docker Compose sets these for the web service)
# STATIC_ROOT=/app/staticfiles
# MEDIA_ROOT=/app/media

# --- Production email (SMTP) — used when DEBUG=False unless EMAIL_BACKEND is set ---
# Most providers: port 587, EMAIL_USE_TLS=True, EMAIL_USE_SSL=False
# Some providers: port 465, EMAIL_USE_TLS=False, EMAIL_USE_SSL=True
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# EMAIL_TIMEOUT=30
# DEFAULT_FROM_EMAIL="Jongsong <noreply@yourdomain.com>"
# SERVER_EMAIL=errors@yourdomain.com